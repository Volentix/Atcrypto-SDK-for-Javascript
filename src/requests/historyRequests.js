import axios from "axios";

const createHistoryRequest = (options={}) => {

}



const createTplsHistoryRequest=({
    config,
    token,
    key,
    data
})=>{
    return new Promise((resolve,reject)=>{
        const url=config.cache+""
        axios.get()
    })
}